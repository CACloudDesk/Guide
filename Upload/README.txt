Drop pictures and PDFs here.
Run the script to move them into the correct folders automatically:

  From project root (Guide):   ./scripts/move-uploads-to-folders.sh

Images → assets/
PDFs   → assets/
(Other files stay in Upload unless you add rules in the script.)
